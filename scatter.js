/** @format */

export const drawPoints = (
  scatterPlotLayer,
  data,
  xScale,
  yScale,
  div,
  scatterPlotWidth,
  scatterPlotHeight,
) => {
  console.log("This function plots the points in the scatter plot");
};
